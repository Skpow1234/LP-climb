{
  "name": "lp-climb-workspace",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "workspaces": [
    "packages/*"
  ],
  "engines": {
    "node": ">=20",
    "bun": ">=1.1.0"
  },
  "scripts": {
    "dev": "bun --cwd packages/api run dev",
    "build": "bun --cwd packages/api run build",
    "start": "bun --cwd packages/api run start",
    "lint": "bun --cwd packages/api run lint",
    "format": "bun --cwd packages/api run format",
    "demo": "bun --cwd packages/demo run dev",

    "dev:npm": "npm --workspace packages/api run dev",
    "build:npm": "npm --workspace packages/types run build && npm --workspace packages/core run build && npm --workspace packages/themes run build && npm --workspace packages/github-contrib run build && npm --workspace packages/svg-creator run build && npm --workspace packages/api run build && npm --workspace packages/action run build",
    "lint:npm": "npm --workspace packages/api run lint",
    "format:npm": "npm --workspace packages/api run format"
    ,
    "theme-previews": "node scripts/generate-theme-previews.mjs"
  },
  "devDependencies": {}
}
